export const metadata = {
  title: "Confirmar Presença",
  description: "Confirmar presença no Casamento de Fabio e Martha",
};

export default function GiftsLayou({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}</div>;
}
