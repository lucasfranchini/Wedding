export const metadata = {
  title: "Lista de Presentes",
  description: "Lista de Presentes do Casamento de Fabio e Martha",
};

export default function GiftsLayou({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children}s</div>;
}
